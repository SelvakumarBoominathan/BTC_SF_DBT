version: 2

models:
  - name: stg_btc
    columns:
      - name: HASH_KEY
        tests:
          - not_null
          - unique
  - name: whale_alert
    columns:
      - name: output_address
        tests:
          - assert_valid_btc_address
